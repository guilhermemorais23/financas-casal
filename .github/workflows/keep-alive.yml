name: Manter backend acordado

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Chama a API do Render pra evitar o sleep do plano free
        run: |
          curl -s -o /dev/null -w "status: %{http_code}\n" https://financas-casal-m496.onrender.com/api/me
